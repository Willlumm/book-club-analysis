[project]
name = "book-club-analysis"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=1.17.1",
    "ruff>=0.12.7",
]

[tool.ruff.lint]
select = ["ALL"] 
ignore = []

docstring-code-format = false
